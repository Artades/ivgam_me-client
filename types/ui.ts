export enum EButtonSizes {
	SMALL = "small",
	SMALLER = "smaller",
	DEFAULT = "default",
	LARGER = "larger",
	LARGE = "large",
}

export enum EButtonVariants {
	PRIMARY = "primary",
	OUTLINE = "outline",
	GHOST = "ghost",
}
